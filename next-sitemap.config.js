/** @type {import('next-sitemap').IConfig} */
const config = {
    siteUrl: 'https://yourwebsite.com', // اپنی ویب سائٹ کا اصل URL یہاں ڈالیں
    generateRobotsTxt: true, // robots.txt بھی بنائے گا
    sitemapSize: 5000,
  };
  
  module.exports = config;
  